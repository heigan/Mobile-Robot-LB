# Мобильные роботы

**Курс/семестр:** 1 курс магистратуры, 2 семестр (весна 2026 года)  
**Номер лабораторной:** 1-3  
**Выполнил:** Лащёнов А.Д., студент гр. 8ЕМ51   
**Преподаватель по лабораторным работам:** Поберёзкин Никита  
**Преподаватель по курсовой работе:** Беляев Александр Сергеевич   
**Год:** Томск, 2026

---
## Описание проекта

Проект реализует систему автономной навигации мобильного робота в замкнутой рабочей области размером 2200×2200 мм. В поле присутсвуют препядвия в виде чёрных коробок с цветными стороанми (красный, зелёный, синий). Так же, над рабочей зоной установленна камера. 

В работе используется робот Robotino (голономная платформа с тремя омниколёсами). Robotino работает как сервер, принимающий команды через встроенный HTTP API. На роботе установен ArUco-маркер (словарь DICT_6X6_50)

---
## Ссылки на отчёты
- [Моб. роб_ЛБ1.pdf](Отчёты/Моб.роб_ЛБ1.pdf)
- [Моб. роб_ЛБ2.pdf](Отчёты/Моб.роб_ЛБ2.pdf)
- [Моб. роб_ЛБ3.pdf](Отчёты/Моб.роб_ЛБ3.pdf)
- [Моб. роб_Курсовая.pdf](Отчёты/Моб.роб_Курсовая.pdf)
---

## Описание директории репозитория
- [Отчёты](Отчёты): Папка с отчётами
- [screenshots](screenshots): Содержит в себе изображения полученные при проведенииработ.
- [LB_1.py](LB_1.py): Программный файл, для выполенния лабораторной работы №1.
- [LB_2.py](LB_2.py): Программный файл, для выполенния лабораторной работы №2.
- [LB_3.py](LB_3.py): Программный файл, для выполенния лабораторной работы №3.
- [LB_4_spline.py](LB_4_spline.py): Программный файл, для выполенния курсовой работы.
- [parameters.yaml](parameters.yaml): Конфигурационный файл, который содержит в себе значения настраиваемых параметров для работы всего проекта.
- [measurements.yaml](measurements.yaml): Файл для сохранение резульатов автоматичсеких тестов
- [robot_func.py](robot_func.py): Программный файл с функциями для взаимодействия с Robotino.
- [vision_func.py](vision_func.py): Программный файл с функциями для работы с видеокамерой (находится над полем).
- [field_boundary.py](field_boundary.py): Программный файл для определения границ и размеров рабочей зоны
- [Obstacle_detection.py](Obstacle_detection.py) : Программный файл для настройки цветов препядствий
- [path_planner.py](path_planner.py): Программный файл с функциями для реализации алгоритмов планирования и алгоритмов построения пути.
- [config_loader.py](config_loader.py): Программный файл с функциями для загрузки конфигурации из YAML
- [test_manager.py](test_manager.py): Программный файл с функциями тестированием
- [metrics_utils.py](metrics_utils.py): Программный файл с функциями расчёта метрик

## Подготовка к выполению лабораторных работ
Перед выполенние основных задач лаборатоных работ необходимо провести подготовку
0. Подлючится к камере по USB-проводу
1. Необходимо инициализировать виртуальное пространство **venv** и загрузить необходимые библиотеки:

```bash
# Создание виртуального пространства с Python 3.13
python -m venv .venv
.venv\Scripts\activate

# Установка библиотек для работы (если нет requirements.txt)
pip install numpy opencv-python ...

# Установка библиотек с файла requirements.txt
pip install -r requirements.txt
```

2. Запустить файл [field_boundary.py] и последоватьно укаазать угловй точки поля. Первая точка (угол) пределяет начало системы координат. Вторая точка определяет направление оси абсцисс. Последняя точка определяет направление оси ординат [detect_work_zone_corners.png](detect_work_zone_corners.png). После вводы точек необхоидмо указать длинну и ширину рабочей области, а так же указать радиус робота 

3. Для настройки обнаружения препядвий запускаем файл [Obstacle_detection.py](Obstacle_detection.py). Необходимо настроить диапозоны цветов для обнаружения препядвий, [safety_margin] и [obs_radius_m]. 

4. Так же в [parameters.yaml](parameters.yaml) необходи указать параметры робота для подкючения

``` bash
robot:
  robot_id: "192.168.0.1"   # id для подключения робота
  robot_port: 80            # номер порта для подключения

```

## Лабораторная работа №1

### Задача 
Требуется реализовать движение робота из точки “А” в точку “Б” с алгоритмом объезда препятствий с выводом координат робота и отображением пройденного пути. 

### Определение местоположения и ориентации робота в глобальной системе
Локализация мобильного робота осуществляется методом визуальной навигации с использованием двумерных маркеров ArUco. Данный подход обеспечивает высокую точность определения как позиции, так и угла поворота платформы в реальном времени. Более подробно алгоритм описан в [отчёте](Отчёты/Моб.роб_ЛБ1.pdf)

[Определение системы координат робота и его координат в глобальной системе](screenshots\local_robot.png)

### Детектирование препятствий
Обнаружение препятствий в системе выполняется на основе цветовой фильтрации в цветовом пространстве HSV.
Алгоритм построения маски и извлечения данных о препятствиях включает следующие шаги:
1.	HSV-фильтрация. Исходный кадр преобразуется из пространства BGR в HSV. К кадру последовательно применяется функция cv2.inRange для заданных цветовых диапазонов (например, красный, зеленый, синий, черный), в результате чего формируется бинарная маска потенциальных препятствий.
2.	Исключение робота и проверка границ. Поскольку ArUco-маркер на роботе может быть ошибочно классифицирован как препятствие (особенно при детектировании черного цвета), на бинарную маску программно накладывается черная окружность в координатах центра робота, «стирая» его из поля поиска.
3.	Морфологическая очистка. Для устранения мелкого шума и заполнения внутренних пустот в объектах к маске применяются операции открытия (cv2.MORPH_OPEN с ядром 5×5, 2 итерации) и закрытия (cv2.MORPH_CLOSE с ядром 5×5, 4 итерации).
4.	Выделение контуров и фильтрация по площади. С помощью функции cv2.findContours (с флагами RETR_EXTERNAL и CHAIN_APPROX_SIMPLE) извлекаются внешние границы объектов. Контуры, площадь которых меньше порогового значения MIN_OBSTACLE_AREA (300 пикселей), отбрасываются как незначительный шум.
5.	Определение центра и перспективное преобразование. Для оставшихся контуров вычисляются пространственные моменты (cv2.moments) для нахождения центра масс в пиксельных координатах. Затем эти координаты преобразуются в вид сверху (в метры) с помощью матрицы перспективного преобразования cv2.perspectiveTransform.
6.	Инфляция (раздувание) препятствий. На этапе планирования маршрута (path_planner.py) создается дискретная сетка проходимости. Ячейки, соответствующие препятствиям, а также границы поля, принудительно помечаются как непроходимые с учетом эффективного радиуса: суммы радиуса робота (obs_radius_m) и заданного безопасного отступа (safety_margin). Это гарантирует, что центр робота физически не сможет приблизиться к препятствию на опасное расстояние.

[Вид окна визуализации ](LB_1_window.png)

### Построение маршрута движения
На основе данных детектирования препятствий формируется бинарная карта проходимости рабочего поля. Пространство дискретизируется на регулярную сетку с заданным размером ячейки (cell_size). 
Для обеспечения физической безопасности робота и компенсации погрешностей применяется операция расширения (inflation): зоны препятствий и границы рабочего поля программно увеличиваются на величину эффективного радиуса, равного сумме радиуса робота [obs_radius_m] и заданного безопасного отступа [safety_margin]. 

Для поиска оптимальной траектории в качестве базового используется алгоритм A* (A-star)*
A* - алгоритм поиска по первому наилучшему совпадению на графе, который находит маршрут с наименьшей стоимостью от одной вершины (начальной) к другой (целевой, конечной). Стоимость определяется как сумма функции стоимости достижения рассматриваемой вершины (x) из начальной (обычно обозначается как g(x) и может быть как эвристической, так и нет), и функции эвристической оценки расстояния от рассматриваемой вершины к конечной (обозначается как h(x)).

Архитектура планировщика включает механизмы обработки критических ситуаций (edge cases) для обеспечения отказоустойчивости системы:
1.	Недостижимость цели: если целевая ячейка или все подходы к ней заблокированы препятствиями, алгоритм исчерпывает пространство поиска и возвращает пустой маршрут. В этом случае робот инициирует остановку, а в консоль выводится сообщение о невозможности построения маршрута.
2.	Виртуальное застревание на старте: из-за погрешностей детектирования или агрессивных настроек безопасного отступа стартовая ячейка робота может быть ошибочно классифицирована как препятствие. Для предотвращения сбоя реализован алгоритм поиска в ширину (BFS), который сканирует окрестность робота (в радиусе нескольких ячеек) для поиска ближайшей свободной ячейки. 

### Расчёт скоростей движения
Вектор скорости робота формируется на основе глобального пути с помощью релейного управления: при дистанции больше пороговой, скорость практически загуляется.
Полученный в глобальной системе координат рабочей области вектор скорости “velocity” должен быть пересчитан в систему координат, связанную с роботом. Следующий код:

```robot_func.py
def calculate_robot_velocities(robot_data: dict, next_wp: tuple, nav_config: dict) -> tuple:
    """
    Вычисляет линейные скорости робота (Vx, Vy) в его локальной системе координат
    для движения к следующей точке пути.
    
    Входы:
        robot_data: словарь с ключами 'center_m' (x, y в метрах) и 'pts_top' (точки маркера)
        next_wp: кортеж (x, y) целевой точки пути в метрах
        nav_config: словарь с ключами 'speed_far_m_s', 'speed_near_m_s', 'speed_threshold_m'
        
    Выходы:
        (Vx_robot, Vy_robot): скорости в м/с
    """
    rx, ry = robot_data["center_m"]
    wx, wy = next_wp
    
    # 1. Расстояние до точки
    dist_to_wp = math.hypot(wx - rx, wy - ry)
    
    # 2. Выбор скорости
    V = nav_config["speed_far_m_s"] if dist_to_wp >= nav_config["speed_threshold_m"] else nav_config["speed_near_m_s"]
    
    # 3. Угол к цели в глобальной системе поля
    angle_to_wp = math.atan2(wy - ry, wx - rx)
    
    # 4. Скорости в глобальной системе поля
    Vx_field = V * math.cos(angle_to_wp)
    Vy_field = V * math.sin(angle_to_wp)
    
    # 5. Ориентация робота (учет поворота маркера)
    pts_top = robot_data["pts_top"]
    marker_heading = math.atan2(pts_top[1][1] - pts_top[0][1], pts_top[1][0] - pts_top[0][0])
    psi = marker_heading - math.pi / 2  # По условию: Ось X робота совпадает с Осью Y маркера
    
    # 6. Поворот вектора скорости из поля в локальную систему робота
    cos_psi = math.cos(psi)
    sin_psi = math.sin(psi)
    
    Vx_robot = Vx_field * cos_psi + Vy_field * sin_psi
    Vy_robot = -Vx_field * sin_psi + Vy_field * cos_psi
    
    return Vx_robot, Vy_robot
```
### Заезд
Для проведения анализа работа алгоритма был проведён автоматический заезд показанный на рисунке
[Проезд по алгоритму A*](astar_forward_0.4_0.2_0.3_1.9.png)

Во время заезда были сняты следующие метрики:
- Время прохождения маршрута: 28.03 с
- Планируемый путь: 3,023 мм (длина глобального маршрута, построенного алгоритмом A*).
- Пройденный путь: 3,027 м (фактическая длина траектории робота).
- MSE: 438,01 мм2 (среднеквадратичное отклонение траектории от запланированного пути).
- R²: 0,9989 (коэффициент детерминации).

### Итог по лаборатоной работе №1
В результате выполнения лабораторной работы была реализована система автономной навигации мобильного робота Robotino в замкнутой рабочей области с известными статическими препятствиями. Для распознавания препятствий и робота используется видеопоток с камеры, расположенной над полем. В качестве планировщика маршрута был интегрирован A*, который позволяет при планировании пути избегать препятствия. По результатам проезда была достигнута высокая точность следования траектории. 

## Лабораторная работа №2
Основая часть работы аналогична лабораторной работе №1
Однако теперь, изменяя в [parameters.yaml](parameters.yaml) параметр [planner_type] нужно сравнить несколько алгоритмов движения. Были взяты 3 алгоритма:
- A* (реализованный в ЛБ №1)
- Двухнарпавленный алгоритм Дейкстры
- Жыдный алгоритм

Для каждого из этих алгоритмов был проведёт автоматический тест с фиксированными точками старта и финиша

[Проезд по алгоритму A*](screenshots\astar_forward_0.4_0.2_0.3_1.9.png)
*Проезд по алгоритму A * .*
[Проезд по двухнарпавленный алгоритм Дейкстры*](screenshots\bi_dijkstra_forward_0.4_0.2_0.3_1.9.png)
*Проезд по двухнарпавленный алгоритм Дейкстры.*
[Проезд по жадному алгоритму](screenshots\greedy_forward_0.4_0.2_0.3_1.9.png)
*Проезд по жадному алгоритму.*

В итоге получились следующтие метрики
*Сравнительная таблица.*
| Алгоритм | Планируемая длина, м | Фактическая длина, м | Время в пути, с | MSE, мм² | R² |
| :--- | :---: | :---: | :---: | :---: | :---: |
| **A*** | 3.02 | 3.03 | 28.03 | 438.01 | 0.9989 |
| **Bi-Dijkstra** | 3.04 | 3.05 | 27.74 | 406.56 | 0.9990 |
| **Greedy** | 3.19 | 3.37 | 29.75 | 375.65 | 0.9991 |

### Итог по лабораторной работе №2

В результате выполнения лабораторной работы проведен сравнительный анализ трех алгоритмов планирования пути: A*, двунаправленного Дейкстры и жадного алгоритма (Greedy).
На основе экспериментальных данных установлено, что для навигации в замкнутом полигоне со статическими препятствиями наиболее целесообразно использовать алгоритм A*. Он обеспечивает наилучший баланс между оптимальностью маршрута и временем движения, минимизируя физические затраты робота.
Двунаправленный алгоритм Дейкстры выступает равноценной альтернативой: он гарантирует строгую математическую оптимальность без использования эвристик, демонстрируя сопоставимые с A* показатели скорости (~27.7 с) и высокой точности следования траектории (R² > 0.998).
Жадный алгоритм, несмотря на высокую скорость вычислений и хорошую отрабатываемость системой управления (R² = 0.9991), формирует субоптимальные, извилистые траектории. Это приводит к значительному увеличению фактической длины пути и общего времени движения, что делает его применение нецелесообразным в задачах, где критична общая эффективность маршрута.

## Лабораторная работа №3
Основая часть работы аналогична лабораторной работе №1

### Динамическая перестройка маршрута
В разработанной системе реализован механизм динамической перестройки маршрута, позволяющий мобильному роботу адаптироваться к появлению новых или перемещению существующих препятствий в реальном времени без необходимости полной остановки и ручного вмешательства.
Процесс динамического планирования работает в основном цикле программы и базируется на непрерывном мониторинге состояния текущего маршрута (current_path) и актуальных данных детектирования препятствий (obstacles). Инициация перестройки маршрута происходит при выполнении одного из трех условий:
1.	Первичное планирование: если маршрут отсутствует (not current_path), но система имеет данные о текущем положении робота и целевой точке, инициируется первоначальный расчет траектории.
2.	Реакция на блокировку пути: на каждом кадре вызывается оптимизированная функция is_path_blocked. Для снижения вычислительной нагрузки она проверяет не весь маршрут, а только ближайшие 5 путевых точек (waypoints). Если евклидово расстояние от любой из этих точек до центра обнаруженного препятствия становится меньше эффективного радиуса безопасности (safety_radius, равного сумме радиуса препятствия obs_radius_m и безопасного отступа safety_margin), текущий маршрут признается непроходимым.
3.	Периодическая адаптация: для обеспечения плавного реагирования на медленно движущиеся объекты, которые еще не вошли в критическую зону, но уже изменили конфигурацию поля, реализован принудительный триггер перестройки каждые 20 кадров (frame_count % 20 == 0).

### Итог по лабораторной работе №3
В результате выполнения лабораторной работы была реализована система автономной навигации мобильного робота Robotino в замкнутой рабочей области с известными статическими/динамическими препятствиями. Для данной лабораторной работы был реализован алгоритм, который вместе с обновлением кадра производит переоценку маршрута. В результате чего робот смог перестраивать маршрут во время движения препятствий


